# ESTADO — 2026-09-07 (cierre de sesión, seguro para reiniciar VS Code)

> Continúa de `docs/m3/ESTADO_2026-09-06.md`. Rama `v2`. Local == `origin/v2` @ **`c6126b6`**.
> Working tree limpio salvo 3 untracked que NO se tocan (ver §7).

---

## 1. Estado macro

```
Rama:        v2 → origin/v2 @ c6126b6 (todo pusheado)
CI último:   success (6/6 jobs reales; Chromatic falla siempre por config Vite/webpack — continue-on-error, NO bloquea)
Producción:  excalibur-six-chi.vercel.app @ c6126b6 — deploy success, 42 páginas SSG
Verificación local: lint 0 · typecheck 0 · test 284/284 (38 files) · build 0 · build-storybook 0
```

Esta sesión ejecutó **Fases A→F** de la estructura de casos + refactor de la portada
(gap v1→v2). Detalle abajo.

---

## 2. Lo que shipó esta sesión (commits `3cb3219` → `c6126b6`)

| Fase | Commit | Qué |
|---|---|---|
| **A** | `3cb3219` | `CasePageData` v1.1.0 — schema extendido retrocompat (contrato movido a `src/components/templates/CasePage/types.ts`). |
| **B** | `822dd05`, `c14e339` | 5 componentes LOCKED: `EX-v2-ATOM-009` EvidenceLink, `EX-v2-MOL-009` EvidenceLinks, `EX-v2-MOL-010` WorkTestCard, `EX-v2-MOL-011` DecisionTable, `EX-v2-MOL-012` AiDisclosure. **No tocan `ProjectCard` (EX-v2-MOL-002)**. |
| **C** | `d86135e` | `CasePage` render extendido (bloques condicionales): Valor, STAR-L (`<h2>` por sección), DecisionTable, cuerpo por variante (`evidencia-viva` → EvidenceLinks / `documento-estrategico` → secciones numeradas), AiDisclosure, footer disclaimer exacto, badges. `validateCaseData.ts` (R-2 build-time). → `EX-v2-TMPL-002` **v1.1.0** LOCKED. |
| **D** | `eac885d` | `src/content/cases/*.ts` — 4 work-tests + meta-caso + sección FDN-Momentum-2 (`Momentum2Section`). Contenido verbatim de `WORKTEST_CASES.md` / `CASE_PAGE_CONTENT_STRUCTURE.md`. `cases.test.ts`. |
| reconc. | `63ff5ba`, `cedafa0` | Cierre C1-C8 con verificación de fuente primaria (ver §5). |
| **E** | `98bb2e7` | 8 casos de cliente en `src/content/cases/clients/*.ts` + páginas: `EX-v2-PAGE-003` `/metodologia`, `-004` `/pruebas-tecnicas/[slug]`, `-005` `/pruebas-tecnicas`, `-006` `/excalibur`; `/casos/[slug]` (`PAGE-002` v1.1.0) migrado a los 8 módulos + sección FDN-M2 vía `appendixSection` (CasePage → **v1.1.1**). Redirect 301 `/casos/solidaria` → `/pruebas-tecnicas/solidaria`. `sitemap.ts` actualizado. |
| **F** | `c6126b6` | Portada v1→v2: `StackSection` **v2.0.0** (BREAKING — sin `level:number` fabricado), `EX-v2-ORG-009` FaqSection, `-010` FlagshipSection, `-011` IndustriesSection. `HomeTemplate` **v1.1.0** (9 secciones, orden de v1). Contenido en `src/content/home.ts`. |

---

## 3. Taxonomía de casos (decisión IC — memoria `[[case-taxonomy]]`)

Dos árboles que **no cruzan contenido ni URLs**:

- **`/pruebas-tecnicas/*`** — ejercicios no remunerados: FleetControl, Solidaria (diagnóstico autodirigido), BCS, Codesa.
- **`/casos/*`** — experiencia profesional pagada: correos-chile, bbva, fdn, lasalle, fid-seguros, sured, parking-ruedaz, siclo-idpay.
- **`/excalibur`** — meta-caso (`caseType: 'meta'`).
- **FDN Momentum 2** — sección dentro de `/casos/fdn` (badge "📐 Estimación propia", ancla `#momentum-2-propuesta`, disclaimer propio, R-8). NO ruta aparte.

---

## 4. Pendientes por categoría

### 4.1 Navegación (lo más importante — hace alcanzable todo lo de Fase E)
- **`CasesSection` → `ProjectCard` no llevan `href`.** El home NO enlaza a `/casos/[slug]` (salvo el flagship, que sí enlaza a `/casos/correos-chile`). Los otros 7 case-cards no navegan.
- **Nav/header sin enlaces** a `/pruebas-tecnicas`, `/metodologia`, `/excalibur` — son alcanzables solo por URL directa (aunque las páginas existen y funcionan).

### 4.2 Contenido v1 no migrado
- **Evidence Explorer de 24 proyectos** (v1 `main:public/original/js/explorer.js` + `data/evidence.json`): grilla filtrable por sector, 2013-2026 (El Tiempo, Ecopetrol, Colsanitas, REDEBAN, DNP-SINERGIA, PROCOLOMBIA, TVS, UdeA, CMC, PQEB, ADL/Mentu, + "Consolidado Histórico"). **No existe equivalente en v2.** Vive en `docs/Career_Dossier/` y `main:public/original/data/evidence.json`. `src/data/evidence-dna.json` tiene 7 entradas y no se renderiza.
- **Siclo/IDPay**: v1 tenía link "🔄 Data + UI Process" → Drive `12hHKXuPCh88e-yRMdsEl_Sfv-l245Ozz`. `siclo-idpay.ts` no tiene ese `accessLink`.
- **LinkedIn**: v1 usa `linkedin.com/in/mauropx`; `src/components/infra/JsonLd` (`PersonJsonLd`) usa `linkedin.com/in/leomaurogomez`. Uno está mal — confirmar cuál.
- **CV descargable** (`main` → Drive `1-HhZ_p2aH0kQ5uuoRTVsGYzEp3m4tSV3`): v2 no lo ofrece. Puede ser intencional (`abd9c7b` "unifica los 2 CV en archivo maestro privado") — confirmar.

### 4.3 Copy / calidad
- **`/metodologia`** es v1: ~35 entradas de 1-2 líneas por slug + insumo Guidewire. Copywriting profundo pendiente.
- **Home `caseProjects`**: los tags de filtro (`symptomTags`/`roleTags`) los curó Claude en `CASE_META` (en `page.tsx` y en `HomeTemplate.stories.tsx`) — vale revisión de contenido.
- **`page.tsx` `titanModules`**: descripción de M3 dice "26/26 componentes LOCKED" (dato viejo — hoy hay ~40). No bloquea.
- **EN diferido (Fase G)**: TODO el contenido de caso + el de `src/content/home.ts` es ES-only. i18n cubre solo el chrome compartido.

### 4.4 Reconciliación — decisiones abiertas del IC (no bloquean, ya aplicadas conservadoras)
- **C1/C2 Codesa**: skill IA quedó **v5.1** (verbatim del entregable). Se agregó el bloque de resiliencia de pagos (Outbox Pattern, trace_id/OpenTelemetry, Circuit Breaker+Bulkhead) verificado en el PDF final.
- **C3 Solidaria**: se mantuvo la abstracción de `WORKTEST_CASES.md` (no se metió "Next.js 16" ni los específicos COGA/MSW de CAPA 3.3 sin confirmar).
- **C4 BBVA "-75% TTM"** y **C6 FID "+70% CX" / "46+ entregables"**: RETIRADOS (sin fuente primaria). Headlines reemplazados por dato verificable.
- **C7/C8 FDN**: "AAA" → **WCAG 2.1 AA** (la AAA es de La Salle). M1 = caso; M2 = sección "Estimación propia".

### 4.5 Higiene `docs/Career_Dossier/` (no urgente)
- `CONSOLIDACION_MAESTRA_…(1).md` ya borrado (`63ff5ba`).
- `SI-CLO [Módulo de Gestión de Convenios].docx.md` — volcado crudo de 1.3 MB + **fantasma NFC/NFD** (aparece siempre como `??`, NO hacer `git add`). Decidir si se saca del repo.
- `CAREER OS SSOT.md` vs `MauroGO_CAREER OS SSOT.md`: el primero es superset (añade CAPA 6 CV-ATS + CAPA 7 scripts de prospección). ¿Sacar del repo del producto?
- `Informe_Evolucion_Madurez_Demos_Tutoriales_BBVA.pdf` — binario 1 MB, permisos 600.

---

## 5. Dónde retomar (orden sugerido)

1. **Navegación (§4.1)** — desbloquea el valor de Fase E. Requiere `href` en `CasesSection`/`ProjectCard` (LOCKED — bump de versión) + entradas de nav en `NavSystem`/`HomeTemplate` a `/pruebas-tecnicas`, `/metodologia`, `/excalibur`.
2. **Evidence Explorer (§4.2)** — portar los 24 proyectos de `evidence.json` a `/trayectoria` (o sección de home) filtrable por sector. Nueva página + `src/content/` (¿`career.ts`?).
3. **Gaps menores (§4.2)** — link Drive de Siclo, handle LinkedIn, decisión CV.
4. **Fase G — i18n EN** del contenido de casos + `home.ts` (tarea de copywriting).
5. **Copy `/metodologia`** más profundo.

---

## 6. Archivos clave nuevos de esta sesión

```
src/components/templates/CasePage/types.ts             — contrato CasePageData v1.1.1 (appendixSection)
src/components/templates/CasePage/validateCaseData.ts  — R-2 build-time
src/components/{atoms/EvidenceLink,molecules/{EvidenceLinks,WorkTestCard,DecisionTable,AiDisclosure}}/
src/components/organisms/{FaqSection,FlagshipSection,IndustriesSection}/
src/content/cases/*.ts            — 4 work-tests + meta + fdn-momentum-2 + index (WORK_TEST_CASES/META_CASE)
src/content/cases/clients/*.ts    — 8 casos de cliente + index (CLIENT_CASES/CLIENT_CASE_SLUGS)
src/content/cases/cases.test.ts   — R-2/R-4/R-6 + metodología sin #slug muertos + reconciliación C4/C7
src/content/methodology.ts        — METHODOLOGY_ENTRIES (~35) + GUIDEWIRE_INSUMO
src/content/home.ts               — FAQ_ITEMS, STACK_CATEGORIES, FLAGSHIP, INDUSTRIES
src/app/[locale]/{pruebas-tecnicas/[slug],pruebas-tecnicas,excalibur,metodologia}/page.tsx
src/app/[locale]/casos/[slug]/FdnMomentum2Section.tsx (+ .test)
next.config.ts                    — redirect 301 /casos/solidaria
```

Governance actualizada: `docs/m3/certificates/COMPONENT_REGISTRY.json` + `docs/m2/spec/TRACEABILITY_MATRIX.md`
(TMPL-001 v1.1.0, TMPL-002 v1.1.1, ORG-005 v2.0.0, +ATOM-009, +MOL-009..012, +ORG-009..011, +PAGE-002..006).

---

## 7. NO tocar / untracked esperado

```
?? docs/Career_Dossier/SI-CLO [Módulo de Gestión de Convenios].docx.md   ← fantasma NFC/NFD (mismo archivo tracked en NFD). NUNCA git add.
?? docs/m3/audit/AUDIT_20260903.md                                        ← auditorías regeneradas por otra sesión. No son de esta sesión.
?? docs/m3/audit/AUDIT_20260904.md
```

Todo lo demás está commiteado y pusheado. **Seguro reiniciar VS Code.**
