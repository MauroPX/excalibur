# ESTADO_2026-09-06.md
# EXCALIBUR v2.0 — Estado de avance y pendientes
# TITAN v7.0 | GSD Status Card | cierre de sesión 2026-09-05/06

---

## 0. Resumen ejecutivo

`feat/v2-analytics-posthog` **mergeada a `v2` (fast-forward, sin conflictos) y desplegada a
producción**. `origin/v2` estaba clavado desde el 2026-07-31; ahora al día con 15+ commits.

```
Producción:  excalibur-six-chi.vercel.app @ v2 (bad6374) — READY, verificado GET / → 200
CI:          workflow "success" (7/8 jobs; Chromatic falla por config preexistente, continue-on-error)
Calidad:     lint 0 · typecheck 0 · test 198/198 (28 archivos) · build 0 · build-storybook 0
Componentes: 29 LOCKED (24 UI + Setup + PostHogProvider + ColorRolesGallery/HUD + ThemeToggle + LanguageToggle)
```

---

## 1. Qué entró a `v2` esta sesión

| # | Bloque | SPEC_ID / artefacto | Estado |
|---|---|---|---|
| 1 | Desbloqueo build/lint | eslint ignora `excalibur-main_V1/`; story name redundante | ✅ |
| 2 | Gobernanza DS | 0 hex/rgba hardcodeado (5 → tokens `*-rgb`), 0 barrel imports MUI (22 archivos + regla ESLint), `ThemeToggle.blueprint.json` reconstruido. **Bug real:** `--md-sys-color-primary-rgb` de light mode tenía el triplete de `inverse-primary` de dark. Detalle: `docs/m3/DS_GOVERNANCE_AUDIT.md` | ✅ |
| 3 | Consolidación SYNC-001 | 12 docs sueltos → 4 bases canónicas + `docs/README.md` (índice) + `drafts/superseded/`. Gate 1 (BH-6) de `EX-v2-MOL-008` cerrado: **molécula, Ola 7, SPEC_ID definitivo**. Log: `docs/m3/SYNC-001_EXECUTION_LOG.md` | ✅ blueprint; Forge bloqueado por A-01 |
| 4 | Analytics | `EX-v2-ANALYTICS-001` LOCKED — ADR-006 (PostHog solo, no dual Amplitude) + `src/lib/analytics.ts` + `PostHogProvider` + `posthog-js@1.426.2`. Wired en layout. **No-op sin las env vars.** | ✅ código; falta config Vercel |
| 5 | Generador de tokens | `scripts/generate-tokens.mjs` + `pnpm tokens:generate` / `tokens:check`. `DESIGN_TOKENS.json` → `tokens.ts` + `globals.css`. Encontró el bug del punto 2. | ✅ (no wireado a CI) |
| 6 | Stories de templates | HomeTemplate (2 stories), CasePage (4) con datos reales. Decorator i18n global en `.storybook/preview.tsx` (arregla throw en runtime que el build no detectaba). | ✅ |
| 7 | Auditoría de contraste | `docs/m3/COLOR_CONTRAST_AUDIT.md` — recálculo WCAG independiente: **42/48 pares AAA** (los 6 restantes = `fixed-dim`, sin uso) + **36/36 combos icono/texto reales AAA**. | ✅ |
| 8 | i18n bilingüe real | `EX-v2-I18N-ROUTING-001` — `src/i18n/routing.ts` + `navigation.ts` + `src/middleware.ts` + rutas bajo `src/app/[locale]/`. `LanguageToggle` (`EX-v2-ATOM-008`, LOCKED). `ThemeToggle` **cableado por fin** (estaba certificado pero nunca se renderizaba). **Bug:** `CasePageNav` apuntaba a `/caso/` singular (nunca funcionó el botón "siguiente caso"). | ✅ mecanismo; contenido ES-only |
| 9 | SEO técnico | `src/app/sitemap.ts` (8 URLs), `src/app/robots.ts`, `generateMetadata` + hreflang, JSON-LD `Person`/`CreativeWork` (`src/components/infra/JsonLd/`). Cerraba el gap de `SEO_AIO_PLAN.md §1` (0% implementado desde M1). | ✅ |
| 10 | FAB revisión de color | `ColorRolesGallery` (`EX-v2-ORG-007`, LOCKED) + `ColorRolesHUD` (`EX-v2-ORG-008`, LOCKED, solo-dev, Alt+C). Story "Foundations/Color". | ✅ |
| 11 | Fix de tipos | TS2322 en 3 tests (`AbstractIntlMessages` vs array JSON). Añadido `pnpm typecheck` (CI lo corría, `pnpm build` no lo cubría). | ✅ |
| 12 | **Fix crítico de deploy** | `middleware.ts` → `src/middleware.ts`. Next.js lo ignora en silencio si vive en la raíz con un dir `src/`. Sin él, `/` daba **404 en toda la app en Vercel**. | ✅ verificado en prod |
| 13 | Docs de estrategia de contenido | `CONTENT_COPY_STRATEGY.md`, `WORKTEST_CASES.md`, `GSD_TASK_CARD_M1_CASE_STRUCTURE.md` (trabajo del IC / otra sesión, mergeado). | ✅ borrador |

---

## 2. Pendientes — por categoría

### 2.1 Decisiones del IC (bloquean trabajo concreto)

| Item | Bloquea | Nota |
|---|---|---|
| **SYNC-001 A-01** — confirmar copy de los 3 ejes de identidad vs. CV maestro (commit `abd9c7b`) | Forge de `EX-v2-MOL-008` (IdentidadProfesional) | Dejado pendiente **explícitamente** por el IC. Nada más depende de esto. |
| **ADR-006 / PostHog** — cargar `NEXT_PUBLIC_POSTHOG_KEY` + `NEXT_PUBLIC_POSTHOG_HOST` en Vercel | Que analytics mida algo | Sin ellas el wrapper es no-op — no rompe build ni runtime, solo no registra eventos. |

### 2.2 Del `EXCALIBUR_IA_EXECUTION_SPEC.md` (revisado — ver crítica completa en el chat de la sesión)

| Item | Estado real | Qué falta |
|---|---|---|
| Contenido de work-tests | Solo **FleetControl + BCS** completos en `WORKTEST_CASES.md`. Commit `6b670a9` dice "completa Codesa + insumo Guidewire/DDD" → **verificar si ya está el contenido de Codesa y el insumo de Metodología**. Solidaria: parcial. | Extracción de PDFs de Codesa (4 docs + research 7.191 líneas) si no está. Fuente de `/metodologia` si el insumo Guidewire/DDD no aterrizó. |
| Schema `CasePageData` | Hoy: `{slug, title, description, tags, metrics[], timeline[], techStack[], nextCase, audienceTags?}`. `company/role/period` viven **dentro de `timeline[]`**, no top-level. Sin `methodology`, `caseType`, `processTransformation`. | Blueprint nuevo con SPEC_IDs para los campos. Extender `CasePageData` **reabre `EX-v2-TMPL-002` (LOCKED)** → revisión de blueprint + bump de cert. |
| Card de work-tests | `GSD_TASK_CARD_M1_CASE_STRUCTURE.md` fase 5 ya fija: **`WorkTestCard` nuevo, NO reutiliza `ProjectCard`** (correcto — evita tocar `EX-v2-MOL-002` LOCKED). | Blueprint + Forge del componente. |
| Rutas nuevas | Ninguna existe. `/metodologia`, `/pruebas-tecnicas/[slug]` (×4), `/pruebas-tecnicas` (índice), meta-caso EXCALIBUR. | Todas bajo `src/app/[locale]/`, con `generateStaticParams` (slug × locale), `generateMetadata` + hreflang, `setRequestLocale`. **Actualizar `src/app/sitemap.ts`** (tiene `CASE_SLUGS = ['fdn','solidaria','bbva']` hardcodeado). Añadir JSON-LD por página. |
| Paralelismo multi-agente | El spec dice "B/C/D/E sin dependencias" — medio cierto. | Archivos **compartidos** que necesitan un paso integrador secuencial: `es.json`/`en.json`, `sitemap.ts`, `COMPONENT_REGISTRY.json`, `TRACEABILITY_MATRIX.md`. |
| No existe página índice `/casos` | Las cards de cliente se renderizan dentro de `CasesSection` (organismo del Home), no en una página lista. | El índice `/pruebas-tecnicas` sería el primer "list page" del proyecto — sin precedente que copiar. |

### 2.3 Contenido / traducción

- **`/en/*` sirve el mecanismo bilingüe pero el contenido de casos y cards sigue en español.** `page.tsx` y `casos/[slug]/page.tsx` tienen el copy de marketing en literales ES (symptomCards, roleCards, caseProjects, texto de cada caso). Traducirlo es tarea de **copywriting/editorial del IC**, no de código. El `useTranslations()` (hero, nav, contact, etc.) **sí** cambia a inglés.

### 2.4 Deuda técnica / config

| Item | Prioridad | Nota |
|---|---|---|
| **Chromatic CI falla siempre** | media | TurboSnap (`onlyChanged: true`) espera `preview-stats.json` de webpack; el Storybook del proyecto usa `@storybook/nextjs-vite`. `continue-on-error: true` → no bloquea. Fix = quitar `onlyChanged` del workflow o generar stats para Vite. **No tocado** (política de no editar CI sin pedido). |
| `pnpm tokens:check` en CI | media | Script listo; falta añadir un step a `.github/workflows/v2.yml`. |
| i18n del `aria-label` de `ThemeToggle` | baja | String fijo. Deuda transversal `EX-v2-I18N-001`. |
| Esquemas medium/high-contrast | baja | Hoy solo base light/dark. Natural si a11y es diferenciador. |
| `docs/m2/spec/TRACEABILITY_MATRIX.md` header "Actualizado: 2026-06-21" | baja | Varias filas de resumen desactualizadas (conteo de tests "171", "Ola activa: 2"). Refresco = tarea de LOCK de ola. |

### 2.5 Backend (Ola 8 — preexistente, no de esta sesión)

- Strapi v5 + Railway + PostgreSQL 16 + pgvector — **no iniciado**. Los 3 casos siguen con datos estáticos (`EX-v2-DATA-001`, parcial 3/20).
- Env vars Vercel pendientes: `STRAPI_API_TOKEN`, `ANTHROPIC_API_KEY`, `GEMINI_API_KEY` + las 2 de PostHog (§2.1).
- `TitanRAGAgent` (`EX-v2-RAG-001`): `api_connected: false` en el registry — el chat no responde de verdad hasta que haya claves.

### 2.6 Limpieza de ramas

Todas estas están **mergeadas a `v2`** (son ancestros de `feat/v2-analytics-posthog`) — borrables local + remoto:
`feat/v2-analytics-posthog`, `fix/v2-build-unblock`, `fix/v2-ds-governance`, `feat/v2-sync-001-blueprints`, `feat/v2-sync-001-gate1-close`.

Ramas viejas preexistentes (no de esta sesión, revisar aparte): `feat/v2-atoms-ola1-final`, `feat/v2-ola2-complete`, `feat/v2-ola5-infra`, `feat/v2-ola6-pages-i18n`, `feat/v2-sprint-abcd`, `feat/v2-titan-rag`.

---

## 3. Dónde retomar

**Camino recomendado para el siguiente sprint (contenido nuevo):**

1. **Ajustar el spec** con los 3 puntos de la §2.2: confirmar contenido de Codesa/Metodología, asignar SPEC_IDs a `WorkTestCard` + rutas nuevas + schema extendido, y añadir los pasos de SEO (`sitemap.ts`, `generateMetadata`, JSON-LD) por página.
2. **Bloque A del spec** ya está hecho (merge + push). Empezar por **Bloque B** (blueprint del schema — no reabrir `EX-v2-TMPL-002` sin revisión; considerar tipo separado `WorkTestData`).
3. En paralelo, cerrar **A-01** (copy identidad) y **ADR-006** (env vars PostHog) — son decisiones cortas del IC que desbloquean 2 frentes.
4. Antes de mergear cualquier página nueva: `pnpm test && pnpm typecheck && pnpm build && pnpm build-storybook` + revisión axe de las rutas nuevas.

---

📍 Momentum: M3 · Cierre de sesión 2026-09-06 · `v2` en producción, verde
